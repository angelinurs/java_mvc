<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="s_note">

	<insert id="insert" parameterType="webEditor.vo.S_note">
		Insert INTO `employees`.`s_note`( title, part, interest, content, reg_time )
		VALUES( #{title}, #{part}, #{m_interest}, #{content}, now() )
	</insert>

</mapper>